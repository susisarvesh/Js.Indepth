//In Undefined the memory is allocated but the value is not assigned.
//In Not Defined the memory is not allocated.
//In Execution Context, the memory is allocated and the pahse will move on to Code Component where In the below example .
console.log(a); //undefined
var a = 10;

console.log(b); //notdefined
